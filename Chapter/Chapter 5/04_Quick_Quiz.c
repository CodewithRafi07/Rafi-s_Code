//use the library function to calculate the ara of a equare with side a
#include <stdio.h>
#include <math.h>

int main(){
    int a = 6;
    printf("the are of this square is %.5f\n" , pow(a , 2)); //Pow is a function. its return Double (f).

    return 0;
}